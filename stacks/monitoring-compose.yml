services:
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ../monitoring/prometheus/proxmox_pbs_targets.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
